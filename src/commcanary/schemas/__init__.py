"""Packaged copies of CommCanary's published JSON Schemas.

The canonical editable sources live in the repository-level ``schemas/``
directory. The release gate copies those exact files here in its isolated build
staging tree and verifies the wheel inventory.
"""
